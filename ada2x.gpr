with "asis.gpr";
with "aws.gpr";
library project Ada2X is
    for Source_Dirs use ("src");
    for Object_Dir use ".obj/" & project'name;
    for Library_Name use "fore";
    for Library_Dir use "lib";
end Ada2X;
